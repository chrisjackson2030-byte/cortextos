
- **codex-safe** (~/cortextos-data/tools/codex-safe): per-account lock wrapper for Codex. Serializes runs per CODEX_HOME (Python fcntl) so concurrent token refreshes can't clobber each other -> fixes 'app_session_terminated'. Use 'codex-safe exec ...' in place of 'codex exec ...'. Added 2026-06-06.

- **carrybench_apply.py** (orgs/main/agents/jarvis/tools/carrybench_apply.py): stands up The Carry Bench AI-persona Shopify store on eudvn4-c0.myshopify.com (token Keychain shopify_monk_admin_token). Creates/updates hero product + SEO metafields + "Everyday Carry" collection; idempotent (matches by handle); dry-run default, --apply to write. Token scopes are read/write_products ONLY -> pages + homepage NOT handled here (need write_content/theme). Separate from the Zoomie shopify_apply.py. Added 2026-06-09.

- **shopify_operator.py** (cortextos-data/tools/shopify_operator.py): self-progressing autonomous driver for the Shopify revenue project (models build_advancer.py + portfolio_director.py ledger pattern). Reads the single-source roadmap state/shopify-roadmap.json, picks the next non-gated dep-met action, executes it (via carrybench_apply.py) or emits a SPAWN directive for a worker, verifies on disk/live (no fabrication), marks done + auto-advances, and appends to state/shopify-operator-log.jsonl. B-gated actions (creatives/payments/scope-clicks) are never auto-done -> written to state/shopify-pending-b.json + surfaced in the morning brief; if all remaining work is B-gated it goes QUIET (no busywork, no ad spend). Modes: default=execute+advance, --dry-run=pick only, --status=report. Runs as the `shopify-operator` cron (every 3h, replaced the old no-op `shopify-worker`). Run: /Users/chrisjackson/cortextos-data/warehouse/.venv/bin/python. Added 2026-06-09.
